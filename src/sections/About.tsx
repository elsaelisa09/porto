import SectionWrapper from '../components/SectionWrapper'

const About = () => {
  return (
    <SectionWrapper id="about" title="About">
      <p>Ringkasan singkat mengenai profil, nilai, dan fokus profesional.</p>
    </SectionWrapper>
  )
}

export default About
